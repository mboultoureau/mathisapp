<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}TakeFood{% endblock %}</title>
        <link rel="stylesheet" href="{{ asset("css/style.css") }}">
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
        <header>
            <p><a href="{{ path('home') }}">TakeFood<a/></p>
            <nav>
                <ul>
                    <li><a href={{ path("home") }}>Accueil</a></li>
                    <li><a href={{ path("categorie.index") }}>Nos produits</a></li>
                </ul>
            </nav>
        </header>
        <main>
            {% block body %}{% endblock %}
        </main>
        {% block javascripts %}{% endblock %}
    </body>
</html>
